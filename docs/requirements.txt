# Documentation build requirements for Read the Docs
# Sphinx and theme dependencies (must match pyproject.toml [tool.poetry.group.docs])
sphinx>=8.1.3
sphinx-book-theme>=1.1.3
sphinx-design>=0.6.1

# Core package dependencies (CPU-only versions for Read the Docs)
# Note: These are mocked in conf.py but still needed for imports
torch>=2.0.0
numpy>=1.21.0

# Optional: only include if actually used in documentation examples
# matplotlib>=3.7.0
# scipy>=1.9.0

# Note: pawX, triton, and torch are mocked in conf.py as they require GPU/compilation
